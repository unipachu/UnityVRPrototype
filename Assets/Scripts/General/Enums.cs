/// <summary>
/// Represents direction on a vertical plane.
/// </summary>
public enum Dir2D {
    Left,
    Right,
    Down,
    Up,
}

public enum Dir3D {
    Left,
    Right,
    Down,
    Up,
    Back,
    Forward,
}

public enum Side {
    Left,
    Right,
}
